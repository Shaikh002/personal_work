name: Upload IG Reel to YouTube

on:
  schedule:
    # 11:30 AM IST → 18:00 UTC
    - cron: '0 6 * * *'
    #  5:00 PM IST → 11:30 UTC
    - cron: '30 11 * * *'
    #  8:30 PM IST → 15:00 UTC
    - cron: '0 15 * * *'
  workflow_dispatch:
    inputs:
      profile:
        description: 'Instagram Profile'
        required: false
        default: 'cyberuniverse.exe'
      upload_limit:
        description: 'Max Reels to Upload'
        required: false
        default: '1'

jobs:
  upload_reel:
    runs-on: ubuntu-latest
    
    env:
      INSTAGRAM_PROFILE: ${{ inputs.profile || 'cyberuniverse.exe' }}
      UPLOAD_LIMIT: ${{ inputs.upload_limit || 1 }}
      IG_COOKIES_JSON: ${{ secrets.IG_COOKIES_JSON }}
      YT_TOKEN_JSON_B64: ${{ secrets.YT_TOKEN_JSON_B64 }}
      CLIENT_SECRETS_B64: ${{ secrets.CLIENT_SECRETS_B64 }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      LIBGL_ALWAYS_SOFTWARE: "1"        # ✅ New
      QT_QPA_PLATFORM: "offscreen" 

    steps:
      - name: 📅 Checkout repository
        uses: actions/checkout@v3

      - name: 🔧 Install GitHub CLI
        run: sudo apt-get install -y gh

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # 💾 Cache pip dependencies
      - name: 💾 Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # 💾 Cache Playwright browsers
      - name: 💾 Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      # 💾 Cache system dependencies (ffmpeg & others)
      - name: 💾 Cache ffmpeg and sysdeps
        uses: actions/cache@v4
        with:
          path: /usr/local/bin/ffmpeg
          key: ${{ runner.os }}-ffmpeg-v1
        id: ffmpeg-cache

      - name: 🧱 Install system dependencies (only if missing)
        if: steps.ffmpeg-cache.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg libnss3 libatk-bridge2.0-0 libxss1 xvfb unzip jq

      - name: 📦 Install Python dependencies & Playwright
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install requests yt-dlp google-api-python-client google-auth google-auth-oauthlib playwright openai opencv-python pillow pytrends
          playwright install --with-deps

      - name: 📥 Download last successful processed_reels.json
        run: |
          echo "🔍 Searching for last successful run..."
          run_id=$(gh run list --workflow "$GITHUB_WORKFLOW" --branch "$GITHUB_REF_NAME" --limit 5 --json databaseId,status | \
            jq -r '.[] | select(.status=="completed") | .databaseId' | head -n 1)

          if [ -n "$run_id" ]; then
            echo "✅ Found previous run ID: $run_id"
            gh run download "$run_id" -n processed-reels -D processed-reels
          else
            echo "⚠️ No previous successful run found."
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 🧩 Move processed_reels.json to root
        run: |
          if [ -f "processed-reels/processed_reels.json" ]; then
            mv processed-reels/processed_reels.json ./processed_reels.json
          else
            echo "⚠️ processed_reels.json not found in artifact"
          fi

      - name: 🩹 Create empty processed_reels.json if missing
        run: |
          if [ ! -f processed_reels.json ]; then
            echo "[]" > processed_reels.json
            curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
              -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
              -d text="⚠️ *Fallback triggered:* processed_reels.json was missing. Recreated empty list." \
              -d parse_mode=Markdown
          fi    

      - name: 🔑 Restore YouTube credentials
        run: |
          echo "${{ env.YT_TOKEN_JSON_B64 }}" | base64 -d > token.json
          echo "${{ env.CLIENT_SECRETS_B64 }}" | base64 -d > client_secrets.json

      - name: ▶️ Run main script with debug logging
        run: |
          xvfb-run --auto-servernum --server-args='-screen 0 1280x720x24' \
            python auto_reels_to_youtube.py | tee log.txt

      - name: 📤 Notify & upload debug logs if no reels
        if: always()
        run: |
          if grep -q "Reels fetched: 0" log.txt; then
            curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
              -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
              -d text="⚠️ *No reels found during GitHub run.* Possible cookie/session/profile issue.\nLogs uploaded." \
              -d parse_mode=Markdown
          elif grep -q "No new reels found" log.txt; then
            curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
              -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
              -d text="ℹ️ *No new reels to upload. All are already processed.*" \
              -d parse_mode=Markdown
          fi

      - name: 📤 Upload debug logs and screenshots
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: debug-logs
          path: |
            debug_*.*  
            log.txt
 
      - name: 📤 Upload processed reels artifact
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: processed-reels
          path: processed_reels.json
          compression-level: 6
 
      - name: 🗰 Final debug on failure
        if: failure()
        run: |
          echo "--- Failure Debug Start ---"
          ls -la debug_reels_error.png || true
          echo "--- processed_reels.json ---"
          cat processed_reels.json || true
          echo "--- END ---"

      - name: ✅ Telegram success alert
        if: success()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="✅ IG → YT Automation Successful 🎉" \
            -d parse_mode=Markdown

      - name: ❌ Telegram failure alert
        if: failure()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="❌ IG → YT Automation Failed. Check log.txt and debug_screenshot." \
            -d parse_mode=Markdown
